apiVersion: v1
kind: Service
metadata:
  name: vllm-sql
  namespace: agile-assistant
  labels:
    app: vllm-sql
    app.kubernetes.io/part-of: agile-assistant
spec:
  type: ClusterIP
  selector:
    app: vllm-sql
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
