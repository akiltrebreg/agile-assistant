apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: agile-assistant
  labels:
    app: api
    app.kubernetes.io/part-of: agile-assistant
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
